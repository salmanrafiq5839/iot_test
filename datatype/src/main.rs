fn main() {
    // all are scalar data type so we donot use datatype with these.
    let age = 25;
    let percantage = 80.5;
    let grade = " A ";
    let passed = true;

    println!("age = {}",age);
    println!("percantage = {}",percantage);
    println!("grade = {}",grade);
    println!("passed = {}",passed);

    let secret_number:i32 =-80; // signed interger
    println!("No = {}",secret_number);

     /* let secret_number:u32 =-80; // unsigned interger
    print!("No = {}",secret_number); */
    
}
